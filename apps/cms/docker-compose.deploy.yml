# Compatibility pointer only — Phase 2 operators use repo-root
# deploy/docker-compose.deploy.yml and deploy/OPERATIONS.md. This file has no
# independent service definitions and must not become a second deployment path.
name: cms

include:
  - path: ../../deploy/docker-compose.deploy.yml
