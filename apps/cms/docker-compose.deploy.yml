# Deprecated path — use repo-root deploy/docker-compose.deploy.yml (Wave 9.1).
# Kept as a pointer for VPS runbooks that still `cd apps/cms`.
name: cms

include:
  - path: ../../deploy/docker-compose.deploy.yml
