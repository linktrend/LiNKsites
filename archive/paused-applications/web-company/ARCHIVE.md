# Archived application: web-company

`web-company` is the paused LiNKtrend corporate-site application, not an
active LiNKsites factory template. W2-07 removes it from `apps/*`, package
workspace discovery, active CI, and deployment while preserving this complete
source snapshot and Git history. It must not be deployed as part of LiNKsites
without a future, explicitly authorized reactivation packet.
