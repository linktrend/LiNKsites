import { defaultTheme } from "../themes/default";

export async function themeFromRequest() {
  // Placeholder: choose theme based on SITE_ID + designSystemVersion + overrides.
  return defaultTheme;
}
