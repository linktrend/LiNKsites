# Provider aggregation v1

AW-07 classifies read-only cached observations as fresh, stale, incomplete, or unavailable and provides bounded backoff metadata. It never represents cache as GitHub truth, authorization, approval, or feature completion.
