# Provider dispatch boundary v1

AW-08 exposes only eligibility. Exact same-org Brain handoff references and a supported activation interface are required; absent activation is unavailable/HOLD. Internal consumer issues, gates, and ledgers are rejected and never dispatched.
