{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://linktrend.local/linksites/ls11/dependencies.schema.json",
  "title": "LS-11 release dependencies",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "kind",
    "packetId",
    "githubIssue",
    "policy",
    "packetCompletion",
    "dependencies"
  ],
  "properties": {
    "schemaVersion": { "const": 1 },
    "kind": { "const": "ls11-release-dependencies" },
    "packetId": { "const": "LS-11" },
    "githubIssue": { "const": 361 },
    "policy": { "const": "fail-closed" },
    "packetCompletion": { "const": true },
    "dependencies": {
      "type": "array",
      "minItems": 2,
      "maxItems": 2,
      "items": { "$ref": "#/$defs/boundDependency" }
    }
  },
  "$defs": {
    "boundDependency": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "required",
        "satisfied",
        "bindingState",
        "placeholderId",
        "repository",
        "commit",
        "tree",
        "reason"
      ],
      "properties": {
        "id": {
          "type": "string",
          "enum": ["ls10-cutover", "harness-h09-conformance"]
        },
        "required": { "const": true },
        "satisfied": { "const": true },
        "bindingState": { "const": "bound" },
        "placeholderId": { "type": "null" },
        "repository": {
          "type": "string",
          "enum": ["linktrend/LiNKsites", "linktrend/LiNKharness"]
        },
        "commit": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
        "tree": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
        "reason": { "type": "string", "minLength": 1 }
      }
    }
  }
}
