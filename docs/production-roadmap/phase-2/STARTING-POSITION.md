# LiNKsites starting position

Evidence refresh: 2026-09-10, Asia/Taipei

This classification deliberately separates repository source, production
configuration and live operation. Historical receipts prove only the exact
thing they record. They do not turn a source result into a live result.

## Exact LiNKsites identities

| Ref | Commit | Tree | Meaning |
| --- | --- | --- | --- |
| `origin/development` | `4b2dbaf5c0ce34076e5d62f8703bbda197dd8ffb` | `e29607c43146decdf81c2f132562745f6a911780` | current protected engineering source |
| `origin/staging` | `b67221f90c1eff6a96592cc7be74390c9b843da2` | `e29607c43146decdf81c2f132562745f6a911780` | same source tree, different promotion commit |
| `origin/main` | `9eb995d7c151aab6aafabd66e91893084e1acdb2` | `fa16550a8d1981a37113c4ba356cbb0ff0a369f8` | older production-source tree |
| accepted Full Suite input | `6bf85741e9b0881e0cf0a15e14a6ef8540ce410f` | recorded by run | GitHub run `34165549526` succeeded; this is not current live proof |

The primary checkout has a user-owned change at
`apps/cms/next-env.d.ts`. The Issue 515 worktree is separate. The user-owned
file is outside this planning candidate and must remain untouched unless a
later preservation packet explicitly isolates it.

## Working

### Repository source

- Protected `development` contains the completed pre-configuration/Profile v2
  engineering baseline: Profile composition, factory catalogue, exact Library
  materialisation, adoption and entitlement identities, Payload models and
  migrations, semantic content promotion, provider adapters, layout-aware
  rendering, routes, SEO/AI projections, Program orchestration, signed
  LiNKautowork boundary, deployment manifests, five Docker build definitions,
  Compose, preflight, smoke, monitoring and recovery tooling.
- The Profile v2 release record binds accepted LiNKsites source to accepted
  LiNKharness H-09 and records an exact-tree Full result and offline rollback
  rehearsal. Its own record explicitly says it does not prove main publication,
  hosting or production deployment.
- GitHub authentication can read the repository, refs, rulesets, issues, pull
  requests and workflow history. Protected source branches have active source
  and transition controls with no configured bypass actors.
- The established Cursor REST dispatcher was checked on 2026-09-10. It read
  account `cursor-001@linktrend.one`, API key name `Codex-001`, exact model
  `grok-4.6` with effort `medium` and Fast `false`, and repository
  `https://github.com/linktrend/LiNKsites`. No worker was created.

### Server capability

- SSH to `linkserver-03` works. Docker 29.8.0 is callable through
  non-interactive `sudo`.
- Seventeen existing containers were observed running and healthy. Existing
  shared Traefik, PostgreSQL, Redis, MinIO and observability services must be
  reused, not replaced.
- Shared networks `linktrend-s03-proxy`, `linktrend-s03-core-private` and
  `linktrend-s03-data` exist.
- PostgreSQL databases and roles named `linksites` and
  `linksites_payload_baseline` exist, and read-only administrative connectivity
  was proven without disclosing credentials.
- Private LiNKsites middleware definitions already exist in the shared Traefik
  dynamic file.

## Existing but needs testing, configuration or repair

### Repository source

- The Profile v2 functionality is substantial and protected, but it must be
  reconciled against the current accepted LiNKlibraries provider plan and
  tested once as the final consolidated source candidate. Earlier local,
  synthetic, candidate and release evidence must not be relabelled as current
  production proof.
- The deployment/runtime path currently assumes a native Revision 2 provider
  when template publishing is ready. It needs exact-current provider handoff
  binding and must support the agreed first operational provider without
  bypassing Library governance.
- `marketing-smb-v1` source bytes and old evidence exist, but the accepted
  LiNKlibraries planning revision classifies the catalogue entry as
  `quarantined` and `selectable: false`. It therefore cannot be used in
  production until LiNKlibraries truthfully requalifies and admits an exact
  release. Local fixtures and older `approved` evidence cannot override this.
- `master-template-type-1` 1.0.0, 2.0.0-a1.1 and 2.0.0 are present in the
  accepted LiNKlibraries plan as source draft/non-selectable. LiNKsites has much
  of the consumer implementation, but exact provider admission, release
  handoff and exact-current paired proof are still required.
- Promotion receipt checks and proposed Issue 511 adversarial changes require
  focused reconciliation so candidate-controlled bytes cannot authorise their
  own promotion.
- Image publication, runtime environment, route hostnames, migration identity,
  backup/restore commands and monitoring definitions exist but require
  exact-current configuration and one end-to-end source acceptance pass.
- Unique work on Issue 480 variants, Issues 490, 492, 508 and 511 and a small
  number of Phase tips has not yet received a final file-level disposition.
  Useful work must be reused or completed, never discarded.
- Current GitHub readback reports 33 open Dependabot alerts: 13 high and 20
  medium across Next.js, Payload, `qs`, `sharp` and `@vitest/mocker`. They need
  one exclusive dependency-security packet to remediate release-blocking
  findings and explicitly disposition findings outside the deployed closure;
  a later Full Suite cannot silently absorb this debt.

### Production configuration

- Server03 already has LiNKsites-named database objects and historical release
  material. Their schema compatibility, ownership, privilege boundaries and
  reuse value require a gated inventory, backup and isolated restore.
- The current image workflow contains private hostname assumptions that were
  not established by the observed Tailscale machine identity. Runtime hostnames
  and private routes must be selected and validated before image publication.
- LiNKautowork contract code exists in LiNKsites. Live endpoint, key IDs,
  grants and receipts must come from the LiNKautowork-owned deployment; until
  then, the agreed manual/file intake and completion adapter may exercise the
  same canonical contract for the first private operational website.
- Platform authentication, claims and shared migration/application receipts
  must be consumed from the Platform-owned task. The accepted Platform source
  plan is an interface input, not proof that every required live surface is
  ready.

## Missing and must be done

### Repository and release

- Publish sanitized copies of the exact upstream schemas, fixtures or wire
  mappings LiNKsites actually consumes, with provenance and compatibility
  ranges, into LiNKsites before source workers depend on them. A Mac-local
  sibling checkout is not worker-visible authority.
- Produce a final source candidate that binds one exact accepted provider,
  Platform and LiNKautowork interfaces, corrected promotion trust, production
  runtime configuration and retained-branch dispositions.
- Reconcile the final dependency/image closure so no unresolved exploitable
  critical/high advisory reaches release and every remaining alert has an
  evidence-backed disposition and owner.
- Pass one exact-candidate Full Suite and one independent consolidated source
  review, then integrate and promote through the governed protected flow.
- Build and publish the five immutable production images and one release
  manifest from exact protected `main`.

### Live production

- `/srv/linktrend/apps/linksites` and `/srv/linktrend/backups/linksites` do not
  exist and the `linksites-foundation` Compose project is not running.
- No active LiNKsites Traefik router, verified runtime secrets file, current
  immutable five-image release, migration receipt, monitoring activation,
  scheduled backup or rollback receipt exists for LiNKsites.
- No current private website has been produced by the live installation from a
  real approved facts package, and there is no live duplicate/recovery proof.
- Full Master Website Template v2 completion still requires exact admitted
  A1/A2/A3 provider inputs, A/B/C/L paired consumer proof, existing-site
  migration/rollback, and live observation on the same installation.

## Unknown until execution-time evidence exists

- Whether external state will drift after this snapshot: protected refs,
  branch tips, queue ownership, current provider candidates, registry digests,
  Platform receipts, LiNKautowork endpoints and Server03 health must be read
  back immediately before use.
- The exact schema/data content and compatibility of existing LiNKsites-named
  databases until the authorised pre-mutation inventory runs.
- The final production hostnames, secret references, tenant/site identifiers,
  data retention and backup policy until configuration packets resolve them.
- The exact founder-approved real facts package and any consent-dependent
  content, accounts or public side effects. These are requested just in time;
  they are not invented planning prerequisites.
- Any defect exposed only by the final exact provider, production-shaped data,
  immutable image build or live network. Such a defect returns to the smallest
  owning packet and cannot be converted into a PASS by changing labels.
