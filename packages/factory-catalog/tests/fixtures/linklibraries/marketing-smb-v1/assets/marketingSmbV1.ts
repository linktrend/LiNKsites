export const marketingSmbV1LibraryAsset = {
  entryId: 'marketing-smb-v1',
  render(page: { title: string }): string {
    return `<main data-template="marketing-smb-v1"><h1>${page.title}</h1></main>`
  },
}
