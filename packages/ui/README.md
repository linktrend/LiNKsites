# Shared Template Assets

Reusable snippets, components, and design tokens that are **copied** into templates or projects. No project should import from this folder at runtime.

- Update assets here first, then vendor them into templates/projects as needed.
- Keep each asset self-contained with any required docs or usage notes.
